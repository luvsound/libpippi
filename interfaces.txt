# Just an outline to help think through refactoring...


pippicore.h: assert.h, math.h, stdio.h, stdlib.h, string.h
    lpfloat_t
    buffer_t

    PI
    PI2

    SINE
    SQUARE
    TRI
    PHASOR
    HANN

    Buffer.scale()
    Buffer.mix()
    Buffer.create()
    Buffer.destroy()

    MemoryPool.init()
    MemoryPool.alloc()

    Interpolation.linear_pos()
    Interpolation.linear()
    Interpolation.hermite_pos()
    Interpolation.hermite()

    Wavetable.create()
    Wavetable.destroy()


soundfile.h: pippicore.h
    SoundFile.write()

window.h: pippicore.h
    Window.create()
    Window.destroy()

pulsarosc.h: pippicore.h
    pulsarosc_t

    PulsarOsc.create()
    PulsarOsc.process()
    PulsarOsc.destroy()

sineosc.h: pippicore.h
    sineosc_t

    SineOsc.create()
    SineOsc.process()
    SineOsc.destroy()


# user-facing header (except for embedded...)
pippi.h: pippicore.h, soundfile.h, window.h, oscs.pulsar.h, oscs.sine.h, ringbuffer.h

