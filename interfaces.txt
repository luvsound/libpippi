# Just an outline to help think through refactoring...

pippicore.h: assert.h, stdlib.h, stdio.h, string.h, math.h
    lpfloat_t
    buffer_t

    PI
    SINE
    SQUARE
    TRI
    PHASOR
    HANN

    Buffer.scale()
    Buffer.mix()

    Buffer.create()
    Buffer.destroy()

interpolation.h: pippicore.h
    Interpolation.linear_pos()
    Interpolation.linear()
    Interpolation.hermite_pos()
    Interpolation.hermite()

soundfile.h: pippicore.h
    SoundFile.write()

wavetable.h: pippicore.h
    Wavetable.create()
    Wavetable.destroy()

window.h: pippicore.h
    Window.create()
    Window.destroy()

pulsar.h: pippicore.h
    pulsar_t

    Pulsar.create()
    Pulsar.process()
    Pulsar.destroy()

sineosc.h: pippicore.h
    sineosc_t

    SineOsc.create()
    SineOsc.process()
    SineOsc.destroy()


# user-facing header (except for embedded...)
pippi.h: pippicore.h, soundfile.h, interpolation.h, wavetable.h, window.h, pulsar.h, sineosc.h

