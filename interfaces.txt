# Just an outline to help think through refactoring...

pippicore.h: assert.h, math.h, stdio.h, stdlib.h, string.h
    lpfloat_t
    buffer_t

    buffer_factory_t
    memorypool_factory_t

    PI
    PI2

    SINE
    SQUARE
    TRI
    PHASOR
    HANN

    Buffer.scale()
    Buffer.mix()
    Buffer.create()
    Buffer.destroy()

    MemoryPool.init()
    MemoryPool.alloc()

    Interpolation.linear_pos()
    Interpolation.linear()
    Interpolation.hermite_pos()
    Interpolation.hermite()

soundfile.h: pippicore.h
    SoundFile.write()

wavetable.h: pippicore.h
    Wavetable.create()
    Wavetable.destroy()

window.h: pippicore.h
    Window.create()
    Window.destroy()

pulsar.h: pippicore.h
    pulsar_t

    Pulsar.create()
    Pulsar.process()
    Pulsar.destroy()

sineosc.h: pippicore.h
    sineosc_t

    SineOsc.create()
    SineOsc.process()
    SineOsc.destroy()


# user-facing header (except for embedded...)
pippi.h: pippicore.h, soundfile.h, wavetable.h, window.h, pulsar.h, sineosc.h

